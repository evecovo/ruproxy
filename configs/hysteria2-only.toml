# Minimal: Hysteria2 only, password auth, self-signed cert
[hysteria2]
listen = "0.0.0.0:443"

[hysteria2.tls]
self_signed_domain = "example.com"

[hysteria2.auth]
type     = "password"
password = "change-me"
